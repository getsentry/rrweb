import { blockClass, canvasManagerMutationCallback, IWindow, listenerHandler, Mirror, blockSelector } from '../../../types';
export default function initCanvas2DMutationObserver(cb: canvasManagerMutationCallback, win: IWindow, blockClass: blockClass, unblockSelector: blockSelector, blockSelector: blockSelector, mirror: Mirror): listenerHandler;
